# Postgres service env. Copy to .env.postgres (gitignored).
# When you integrate Strapi later, create a second role limited to the
# `public` schema rather than letting Strapi use this superuser.

POSTGRES_USER=loyalty_user
POSTGRES_PASSWORD=change-me-to-a-long-random-string
POSTGRES_DB=appdb
